[build-system]
requires = [
    "setuptools>=77",
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "rubis"
version = "2.0.0"
description = "Compute rotating barotropic stellar and planetary models"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    {name = "Pierre S. Houdayer"},
]
license = "GPL-3.0-only"
license-files = ["LICENSE.md"]
keywords = [
    "astrophysics",
    "barotropic models",
    "rotation",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Astronomy",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy",
    "scipy",
]

[project.urls]
Repository = "https://src.koda.cnrs.fr/pierrehoudayer/RUBIS"
Issues = "https://src.koda.cnrs.fr/pierrehoudayer/RUBIS/-/issues"
Mirror = "https://github.com/pierrehoudayer/RUBIS"

[project.optional-dependencies]
plot = [
    "matplotlib",
]
dev = [
    "build",
    "matplotlib",
    "pytest>=8",
]

[tool.setuptools.package-data]
rubis = [
    "data/*.txt",
]

[tool.pytest.ini_options]
testpaths = [
    "tests",
]
addopts = "-ra"
markers = [
    "slow: computationally expensive convergence tests",
    "acceptance: full-scale physical acceptance tests",
]